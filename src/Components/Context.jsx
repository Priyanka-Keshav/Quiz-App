import { createContext } from "react";
export const Category = createContext("Select Category");
export const Difficulty = createContext("Select difficulty");

export const user_ans = createContext([]);
export const answers = createContext([]);
